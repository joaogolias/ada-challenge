import { Typography } from '@mui/material';

export const Header: React.FC = () => {
  return (
    <div className="flex justify-center w-full min-h-[48px] bg-cyan-100">
      <Typography className="self-center"> ADA CHALLENGE - KIBANA </Typography>
    </div>
  );
};
